# config.toml

[dataset]
corpus_path = "~/path/to/my/documents"
max_needles = 100

[needle_llm]
model = "qwen3.5:0.8b"
temperature = 1.5
think = false

[targets]
embedders = ["embeddinggemma", "nomic-embed-text", "qwen3-embedding:0.6b"]
[metrics]
k_vals = [1, 5, 10]

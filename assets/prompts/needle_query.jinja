Text:
{chunk}

If meaningful, write a question that the text can answer. Output only the question or SKIP.

Example:

- 'What is the output of the provided Python code?' is NOT a good query, since it refers to the text.
- 'Which fractions can be represented without round-off error as 64-bit floating-point numbers?' is a good query, since it can be answered, and covers a general topic